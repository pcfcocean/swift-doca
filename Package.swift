// swift-tools-version: 5.7

import PackageDescription

let package = Package(
    name: "Doca",
    products: [
        .library(
            name: "Doca",
            targets: ["Doca"]
        )
    ],
    targets: [
        .target(name: "Doca")
    ]
)
